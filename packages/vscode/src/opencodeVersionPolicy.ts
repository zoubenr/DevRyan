export const TARGET_OPENCODE_VERSION = '1.17.10';

export const OPENCODE_TARGET_INSTALL_COMMAND =
  `curl -fsSL https://opencode.ai/install | bash -s -- --version ${TARGET_OPENCODE_VERSION} --no-modify-path`;
