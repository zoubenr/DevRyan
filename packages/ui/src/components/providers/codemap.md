# packages/ui/src/components/providers/

## Responsibility
Provider-related UI components for model/provider configuration and status.

## Design
Domain components encapsulate provider forms, selectors, and validation feedback.

## Flow
Loads provider catalog/config, renders editable controls, then persists updates.

## Integration
Used by settings sections and wired to lib/providers/api plus stores.
